# Supabase local development configuration.
# Docs: https://supabase.com/docs/guides/local-development
# Run `supabase start` to boot the local stack, then `supabase db reset`
# to apply the migrations in ./migrations.

project_id = "yasmin-quran-tracker"

[api]
enabled = true
port = 54321
schemas = ["public"]
extra_search_path = ["public"]
max_rows = 1000

[db]
port = 54322
major_version = 15

[studio]
enabled = true
port = 54323

[auth]
enabled = false
